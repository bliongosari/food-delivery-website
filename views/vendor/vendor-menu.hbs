<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>INFO30005Project: Vendor Menu</title>
    <link rel="stylesheet" href="/css/vendor/vendor-menu.css">
</head>

<body>
    {{>navVendor}}

    <div class="info">
        {{#if van.image}}
        <img src={{van.image}} alt="" width="200px">
        {{else}}
        <img src="/no-image-available-icon.jpg" alt="" width="200px">
        {{/if}}
        <div class=" van-info">
            <h1>{{van.van_name}}</h1><br>
            <h1>(Van #{{van._id}})</h1>
        </div>
    </div>

    <div class="menu">
        {{#menu}}
        <a class="popup1" href="#details{{this.id}}">
            <div class="menu-item">
                <img src="/{{this.image_url}}" alt="" height="200vh" width="250vw">
                <h1>{{this.name}}</h1>
                <h2>${{this.price}}</h2>
            </div>
        </a>
        <div id="details{{this.id}}" class="pop">
            <div class="popup" id={{this.id}}>
                <img src="/{{this.image_url}}" alt="">
                <a class="closes" href="#">&times;</a>
                <div class="content">
                    <h3 class="itemname"> {{this.name}} </h3>
                    <p class="itemdesc"> {{this.description}} </p>
                </div>
                </a>
            </div>
        </div>
        {{/menu}}
    </div>
</body>

</html>